<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="s1640402.coinzgame.nishtha_coinz">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Coinz"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Loginview">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayGame" />
        <activity android:name=".StockMarket" />
        <activity android:name=".Bank" />
        <activity android:name=".AddFriends"
                  android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".MainView" />
        <activity android:name=".ViewSpareChange" />
        <activity android:name=".ViewWallet" />
        <activity android:name=".ViewFriends"
                  android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".SendingCoins"
                  android:windowSoftInputMode="adjustNothing"></activity>
    </application>

</manifest>